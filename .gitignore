# Dependencies (match a real dir and a worktree symlink; no trailing slash)
node_modules

# Build output
dist/

# Editor / OS
.DS_Store
*.log
.vscode/
.idea/

# Env
.env
.env.local
.env.*.local

# Agent-local state
.claude/

# React Router framework mode
build/
.react-router/

# Prospecting seed data (PII from Nick's master realtor list; NEVER commit).
# The seed script reads this from a local path and PUTs it to Redis.
prospects_seed*.json
.vercel
.env*
